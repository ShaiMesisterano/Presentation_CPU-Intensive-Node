const jimp = require('jimp');
const path = require('path');
const fs = require('fs');

async function init() {
    const files = fs.readdirSync(path.join('.', 'images'));
    console.log(files);
}

init();